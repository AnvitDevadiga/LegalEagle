# Core LangChain (modular, 2025-safe)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# Ollama integration (local LLM + embeddings)
langchain-ollama>=0.1.0
ollama>=0.1.8

# Vector database
langchain-chroma>=0.1.0
chromadb>=0.4.0

# Text splitting
langchain-text-splitters>=0.3.0

# PDF loading
pypdf>=4.0.0

# Terminal UI (Claude Code-style rendering, syntax highlighting, progress bars)
rich>=13.7.0

# Optional (useful for embeddings / future extensions)
sentence-transformers>=2.2.2
